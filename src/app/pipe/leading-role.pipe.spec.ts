import { LeadingRolePipe } from './leading-role.pipe';

describe('LeadingRolePipe', () => {
  it('create an instance', () => {
    const pipe = new LeadingRolePipe();
    expect(pipe).toBeTruthy();
  });
});
